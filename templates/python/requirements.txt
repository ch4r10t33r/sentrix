aiohttp>=3.9.0
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.6.0
eth-account>=0.10.0
web3>=6.15.0

# P2P Discovery (libp2p sidecar bridge)
anyio>=4.3.0
# coincurve is already used in anr.py
coincurve>=13.0.0

# MCP bridge (optional — only needed if using MCPPlugin or serve_as_mcp)
# Wraps any MCP server as a Borgkit agent, or exposes a Borgkit agent as MCP.
# Supports stdio, SSE, and Streamable HTTP transports.
mcp>=1.0.0

# OpenAI Agents SDK (optional — only needed if using OpenAIPlugin)
# pip install openai-agents
# export OPENAI_API_KEY=sk-...
openai-agents>=0.0.3
